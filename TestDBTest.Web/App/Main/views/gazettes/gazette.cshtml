<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
    <div class="">
        <div class="row clearfix card main-content" style="padding-bottom: 20px" ng-controller="app.views.gazettes.gazette as vm">
            <button type="button" data-toggle="modal" style="margin: 10px 10px 10px 20px" class="btn btn-primary btn-circle waves-effect waves-circle waves-float pull-right" ng-click="vm.openTaskCreationModal()">
                <i class="material-icons">add</i>
            </button>
            <table class="table">
                <thead>
                    <tr style="font-size: 15px">
                        <th>Пријава за несакани реакции на медицински помагала</th>
                        <th>Иницијали за име </th>
                        <th>Несаканата Реакција</th>
                        <th>Исход на несакана реакција</th>
                        <th>Поврзаност помеѓу медицинско помагало</th>
                        <th>Суспектно медицинско помагало</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody ng-repeat="gazette in vm.gazettes">
                    <tr>
                        <td>{{gazette.applicationForUnwantedMedDevices}}</td>
                        <td>{{gazette.personFullName}}</td>
                        <td>{{gazette.describeYourAdverseReaction}}</td>
                        <td>{{gazette.outComeOfAverseReaction}}</td>
                        <td>{{gazette.errorDetectionDate}}</td>
                        <td>{{gazette.connectionBetweenAdverseReaction}}</td>
                        <td>{{gazette.medicalDeviceInfoId}}</td>
                        <td class="dropdown">
                            <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">menu</i>
                            </a>
                            <ul class="dropdown-menu pull-right">
                                <li><a class="waves-effect waves-block" ng-click="vm.openGazetteEditModal(gazette)"><i class="material-icons">create</i>Edit</a></li>
                                <li><a href="javascript:void(0);" class="waves-effect waves-block" ng-click="vm.delete(gazette)"><i class="material-icons">delete_sweep</i>Delete</a></li>
                            </ul>
                        </td>
                    </tr>
                </tbody>
            </table>
            <div style="float:right">
                <div class="row">
                    <div class="col-md-4">
                        <a href="javascript:window.print()"><i class="material-icons">save</i></a>
                    </div>
                    <div class="col-md-4">
                        <button class="btn btn-primary" type="button" ng-click="vm.printPdf()">Print</button>
                    </div>
              </div>
        </div>
    </div>
</div>

<style>
    .app-modal-window .modal-dialog {
        width: 80%;
    }
</style>